// Chemistry Constants

const NA = (6.02214076 * Math.pow(10, 23)); // Avogadro's Number, the number of Carbon atoms in exactly 12 grams.


// Element Atomic Mass (amu, g/mol)

const amuH    =   1.00797;    // Hydrogen (H)
const amuHe   =   4.00260;    // Helium (He)
const amuLi   =   6.941;      // Lithium (Li)
const amuBe   =   9.01218;    // Beryllium (Be)
const amuB    =   10.81;      // Boron (B)
const amuC    =   12.011;     // Carbon (C)
const amuN    =   14.0067;    // Nitrogen (N)
const amuO    =   15.9994;    // Oxygen (O)
const amuF    =   18.998403;  // Fluorine (F)
const amuNe   =   20.179;     // Neon (Ne)
const amuNa   =   22.98977;   // Sodium (Na)
const amuMg   =   24.305;     // Magnesium (Mg)
const amuAl   =   26.98154;   // Aluminum (Al)
const amuSi   =   28.0855;    // Silicon (Si)
const amuP    =   30.97376;   // Phosphorus (P)
const amuS    =   32.06;      // Sulfur (S)
const amuCl   =   35.453;     // Chlorine (Cl)
const amuK    =   39.0983;    // Potassium (K)
const amuAr   =   39.948;     // Argon (Ag)
const amuCa   =   40.08;      // Calcium (Ca)
const amuSc   =   44.9559;    // Scandium (Sc)
const amuTi   =   47.90;      // Titanium (Ti)
const amuV    =   50.9415;    // Vanadium (V)
const amuCr   =   51.996;     // Chromium (Cr)
const amuMn   =   54.9380;    // Manganese (Mn)
const amuFe   =   55.847;     // Iron (Fe)
const amuNi   =   58.70;      // Nickel (Ni)
const amuCo   =   58.9332;    // Cobalt (Co)
const amuCu   =   63.546;     // Copper (Cu)
const amuZn   =   65.38;      // Zinc (Zn)
const amuGa   =   69.72;      // Gallium (Ga)
const amuGe   =   72.59;      // Germanium (Ge)
const amuAs   =   74.9216;    // Arsenic (As)
const amuSe   =   78.96;      // Selenium (Se)
const amuBr   =   79.904;     // Bromine (Br)
const amuKr   =   83.80;      // Krypton (Kr)
const amuRb   =   85.4678;    // Rubidium (Rb)
const amuSr   =   87.62;      // Strontium (Sr)
const amuY    =   88.9059;    // Yttrium (Y)
const amuZr   =   91.22;      // Zirconium (Zr)
const amuNb   =   92.9064;    // Niobium (Nb)
const amuMo   =   95.94;      // Molybdenum (Mo)
const amuTc   =   98;         // Technetium (Tc)
const amuRu   =   101.07;     // Ruthenium (Ru)
const amuRh   =   102.9055;   // Rhodium (Rh)
const amuPd   =   106.4;      // Palladium (Pd)
const amuAg   =   107.868;    // Silver (Ag)
const amuCd   =   112.41;     // Cadmium (Cd)
const amuIn   =   114.82;     // Indium (In)
const amuSn   =   118.69;     // Tin (Sn)
const amuSb   =   121.75;     // Antimony (Sb)
const amuI    =   126.9045;   // Iodine (I)
const amuTe   =   127.60;     // Tellurium (Te)
const amuXe   =   131.30;     // Xenon (Xe)
const amuCs   =   132.9054;   // Cesium (Cs)
const amuBa   =   137.33;     // Barium (Ba)
const amuLa   =   138.9055;   // Lanthanum (La)
const amuCe   =   140.12;     // Cerium (Ce)
const amuPr   =   140.9077;   // Praseodymium (Pr)
const amuNd   =   144.24;     // Neodymium (Nd)
const amuPm   =   145;        // Promethium (Pm)
const amuSm   =   150.4;      // Samarium (Sm)
const amuEu   =   151.96;     // Europium (Eu)
const amuGd   =   157.25;     // Gadolinium (Gd)
const amuTb   =   158.9254;   // Terbium (Tb)
const amuDy   =   162.50;     // Dysprosium (Dy)
const amuHo   =   164.9304;   // Holmium (Ho)
const amuEr   =   167.26;     // Erbium (Er)
const amuTm   =   168.9342;   // Thulium (Tm)
const amuYb   =   173.04;     // Ytterbium (Tb)
const amuLu   =   174.967;    // Lutetium (Lu)
const amuHf   =   178.49;     // Hafnium (Hf)
const amuTa   =   180.9479;   // Tantalum (Ta)
const amuW    =   183.85;     // Tungsten (W)
const amuRe   =   186.207;    // Rhenium (Re)
const amuOs   =   190.02;     // Osmium (Os)
const amuIr   =   192.22;     // Iridium (Ir)
const amuPt   =   195.09;     // Platinum (Pt)
const amuAu   =   196.9665;   // Gold (Au)
const amuHg   =   200.59;     // Mercury (Hg)
const amuTl   =   204.37;     // Thallium (Tl)
const amuPb   =   207.2;      // Lead (Pb)
const amuBi   =   208.9804;   // Bismuth (Bi)
const amuPo   =   209;        // Polonium (Po)
const amuAt   =   210;        // Astatine (At)
const amuRn   =   222;        // Radon (Rn)
const amuFr   =   223;        // Francium (Fr)
const amuRa   =   226.0254;   // Radium (Ra)
const amuAc   =   227.0278;   // Actinium (Ac)
const amuPa   =   231.0359;   // Protactinium (Pa)
const amuTh   =   232.0381;   // Thorium (Th)
const amuNp   =   237.0482;   // Neptunium (Np)
const amuU    =   238.029;    // Uranium (U)
const amuPu   =   242;        // Plutonium (Pu)
const amuAm   =   243;        // Americium (Am)
const amuBk   =   247;        // Berkelium (Bk)
const amuCm   =   247;        // Curium (Cm)
const amuNo   =   250;        // Nobelium (No)
const amuCf   =   251;        // Californium (Cf)
const amuEs   =   252;        // Einsteinium (Es)
const amuHs   =   255;        // Hassium (Hs)
const amuMt   =   256;        // Meitnerium (Mt)
const amuFm   =   257;        // Fermium (Fm)
const amuMd   =   258;        // Mendelevium (Md)
const amuLr   =   260;        // Lawrencium (Lr)
const amuRf   =   261;        // Rutherfordium (Rf)
const amuBh   =   262;        // Bohrium (Bh)
const amuDb   =   263;        // Dubnium (Db)
const amuSg   =   269;        // Seaborgium (Sg)
const amuUun  =   272;        // Ununnilium (Uun)
const amuUub  =   277;        // Ununnilium (Uub)
const amuUuq  =   undefined;  // Ununnilium (Uuq)